import { CONFIG } from 'src/global-config';


// ----------------------------------------------------------------------

export const metadata = { title: `Vista de cliente - ${CONFIG.appName}` };

export default function Page() {
  return <div>hello</div>
}
